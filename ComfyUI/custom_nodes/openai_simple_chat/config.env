# Configuración para OpenAI Simple Chat con Vision
# Copia este archivo a .env y configura tu clave API

# Clave API de OpenAI (obtén una en https://platform.openai.com/api-keys)
OPENAI_API_KEY=tu_clave_api_aqui

# Modelo por defecto (con soporte para vision)
DEFAULT_MODEL=gpt-4o-mini

# Configuración de caracteres por defecto
DEFAULT_MAX_CHARACTERS=500

# Prompt del sistema por defecto (siempre en inglés)
DEFAULT_SYSTEM_PROMPT=You are a helpful assistant that always responds in English. Enhance and improve the user's request with professional terminology.

# Configuración de detección de idioma
LANGUAGE_DETECTION_ENABLED=true
